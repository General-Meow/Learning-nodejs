import { isOneEditAway } from "../../src/chapter1/q5";
import { it } from "mocha";
import { expect } from "chai";

describe('isOneEditAway', () => {
    // it('should return true if both words are one or less add away', () => {
    //     let result = isOneEditAway('pale', 'ple');
    //     expect(result).to.be.true;
    // });

    // it('should return false if both words are more than one add away', () => {
    //     let result = isOneEditAway('pale', 'pl');
    //     expect(result).to.be.false;
    // });

    // it('should return true if words are one or less character removed away', () => {
    //     let result = isOneEditAway('pales', 'pale');
    //     expect(result).to.be.true;
    // });

    // it('should return false if words are more than one character removed away', () => {
    //     let result = isOneEditAway('pales', 'pal');
    //     expect(result).to.be.false;
    // });

    // it('should return true if both words are one or less edit away', () => {
    //     let result = isOneEditAway('pale', 'bale');
    //     expect(result).to.be.true;
    // });

    // it('should return false if both words are more than one edit away', () => {
    //     let result = isOneEditAway('pall', 'bale');
    //     expect(result).to.be.false;
    // });
});
